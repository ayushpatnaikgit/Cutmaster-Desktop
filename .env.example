# Port the app is served on (http://localhost:4322).
PORT=4322

# A folder of footage on your computer, mounted read-only at /media in the app.
# In the app, choose "From disk" and enter /media/<file name>.
MEDIA_DIR=./media

# Seconds the agent waits for an answer to its questions before deciding alone.
ASK_TIMEOUT=7200
